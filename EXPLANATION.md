# VoiceFlip — MVP игры для изучения слов голосом

## Как велась работа

### 1. Выбор стека
- **React 19 + Vite 7** — быстрый старт, HMR, мгновенная сборка
- **Web Speech API** — распознавание речи без бэкенда, работает в Chrome/Safari
- **Web Audio API** — синтезированные звуки без внешних файлов (0 зависимостей на аудио)
- **canvas-confetti** — визуальные эффекты при streak-бонусах
- **Inline styles с clamp()** — гарантированная адаптивность без зависимости от CSS-фреймворков

### 2. Архитектура
Всё приложение — один экран с 4 состояниями:
```
start → countdown (3-2-1) → playing → gameover
```

Компоненты:
- `CameraBackground` — фронтальная камера на весь экран как фон
- `Game` — основная логика и UI поверх камеры
- `useSpeechRecognition` — хук для работы с Web Speech API
- `sounds.js` — звуковые эффекты через Web Audio API
- `words.js` — 30 пар слов EN→RU с эмодзи

### 3. Ключевые решения

**Фронтальная камера как фон** — по аналогии с TikTok/Reels, камера занимает весь экран, а UI игры накладывается поверх с полупрозрачным затемнением. При записи экрана получается готовый контент для соцсетей — лицо игрока + интерфейс в одном видео.

**Текстовый ввод как fallback** — Speech API работает не во всех браузерах и контекстах (iframe, HTTP). Добавил текстовое поле, чтобы игра работала везде.

**Inline styles вместо Tailwind** — столкнулся с проблемой: TailwindCSS v4 через Vite-плагин не всегда корректно применял стили при HMR. Перешёл на inline styles с CSS `clamp()` для адаптивности — это гарантирует рендеринг в любых условиях.

**Fuzzy matching** — при распознавании речи допускаю 1 символ ошибки для слов длиннее 3 букв, а также проверяю вхождение подстроки. Это компенсирует неточности Speech API.

**Streak-система** — 3+ правильных подряд = x2 очки + confetti + специальный звук + вибрация. Создаёт азарт и эмоции на видео.

**Звуки без файлов** — все звуки генерируются через Web Audio API (осцилляторы с разными частотами и огибающими). Нет внешних зависимостей, мгновенная загрузка.

### 4. Вирусные элементы
- 🔥 **Streak-счётчик** с огненной анимацией — мотивирует не ошибаться
- 🎊 **Confetti** при каждом 3-м streak — яркий визуальный момент для видео
- ⏱ **Таймер 60 секунд** — создаёт напряжение, пульсирует красным при < 10с
- 📳 **Вибрация** — тактильная обратная связь на мобильных
- 🎵 **Звуки** — мгновенный аудио-фидбек на каждое действие
- 😎 **Эмодзи** — всплывают при правильном ответе
- 📸 **Камера** — лицо игрока на фоне, готовый формат для TikTok

### 5. Трудности

1. **TailwindCSS v4 + Vite** — стили не применялись после изменения конфига. Решение: перешёл на inline styles.

2. **React 19 StrictMode** — двойной вызов эффектов ломал countdown-таймер и lifecycle Speech API. Решение: убрал StrictMode для стабильной работы.

3. **Stale closures в useCallback** — streak-счётчик и обработчик результатов захватывали устаревшие значения из замыканий. Решение: использовал `useRef` для хранения актуальных значений.

4. **Расчёт точности** — формула `score / answers` давала >100% из-за streak-бонусов (2 очка за слово). Решение: добавил отдельный счётчик правильных ответов.

5. **Speech API в iframe** — не работает через прокси/iframe IDE-превью. Решение: добавил текстовый ввод как fallback + тестирование напрямую в Chrome.

## Технические детали

- **Без бэкенда** — всё работает на клиенте
- **0 внешних API** — речь распознаётся браузером (Google Speech Services)
- **Размер бандла** — ~220 КБ (gzip ~70 КБ)
- **Поддержка**: Chrome (десктоп/Android), Safari (iOS 14.5+), Edge — голос. Все браузеры — текстовый ввод.
